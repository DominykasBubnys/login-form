import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    container: {
        flex: 1,
        borderWidth: 2, 
    },

    image: {
        flex: 1,
    }
})

export default Styles;